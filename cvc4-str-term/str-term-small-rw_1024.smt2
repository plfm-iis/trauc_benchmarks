(set-logic QF_S)
(declare-fun x () String)
(declare-fun y () String)
(declare-fun z () Int)
(assert (not (= (str.replace (str.replace "" x y) y x) (str.replace x x (str.replace "" y x)))))
(check-sat)
