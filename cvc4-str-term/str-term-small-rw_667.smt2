(set-logic QF_S)
(declare-fun x () String)
(declare-fun y () String)
(declare-fun z () Int)
(assert (not (= (str.replace y "B" (str.replace y y x)) (str.replace x x (str.replace y "B" x)))))
(check-sat)
