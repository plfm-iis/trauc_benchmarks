(set-logic QF_S)
(declare-fun x () String)
(declare-fun y () String)
(declare-fun z () Int)
(assert (not (= (str.replace y (str.replace "B" y y) x) (str.replace x x (str.replace y "B" x)))))
(check-sat)
