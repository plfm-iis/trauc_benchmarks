(set-logic QF_S)
(declare-fun x () String)
(declare-fun y () String)
(declare-fun z () Int)
(assert (not (= (str.replace "A" (str.replace x y x) "") (str.replace "A" x (str.replace "" y x)))))
(check-sat)
