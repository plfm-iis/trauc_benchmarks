(set-logic QF_S)
(declare-fun x () String)
(declare-fun y () String)
(declare-fun z () Int)
(assert (not (= (str.replace "B" y (str.replace y x "")) (str.replace "B" x (str.replace x y "")))))
(check-sat)
