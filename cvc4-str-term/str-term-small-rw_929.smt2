(set-logic QF_S)
(declare-fun x () String)
(declare-fun y () String)
(declare-fun z () Int)
(assert (not (= (str.replace (str.substr x 0 z) "A" "B") (str.substr (str.replace x "A" "B") 0 z))))
(check-sat)
