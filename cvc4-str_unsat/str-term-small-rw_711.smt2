(set-logic QF_S)
(declare-fun x () String)
(declare-fun y () String)
(declare-fun z () Int)
(assert (not (= (str.replace "A" y (str.replace y y x)) (str.replace x x (str.replace "A" y x)))))
(check-sat)
