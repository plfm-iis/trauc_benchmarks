(set-logic QF_S)
(declare-fun x () String)
(declare-fun y () String)
(declare-fun z () Int)
(assert (not (= (str.replace "" (str.substr "B" 0 z) x) (str.replace "" (str.substr x 0 z) x))))
(check-sat)
